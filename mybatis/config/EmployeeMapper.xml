<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="demo1.EmployeeDao">
<!-- 
namespace:名称空间;指定为接口的全类名
id：唯一标识
resultType：返回值类型
#{id}：从传递过来的参数中取出id值

public Employee getEmpById(Integer id);
 -->
	<select id="getEmpById" resultType="demo1.Employee">
		select id,lastName,email,gender from tbl_employee where id = #{id}
	</select>
	
	<insert id="addEmp" parameterType="employee" useGeneratedKeys="true" keyProperty="id" >
		insert into tbl_employee(lastName,email,gender) values(#{lastName},#{email},#{gender})
	</insert>
	
	<delete id="deleteById" parameterType="demo1.Employee" >
		delete  from tbl_employee where id=#{id}
	</delete>
	
	<update id="updateBYId" parameterType="demo1.Employee" >
		update tbl_employee set lastName=#{lastName} where id=#{id}
	</update>
	
</mapper>